apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "common.names.ingress" . }}
  namespace: {{ .Values.namespace }}
  labels: 
    app.kubernetes.io/name: {{ .Values.commonLabels.name }}
spec:
  tls:
    - hosts:
        - {{ .Values.ingress.keycloakHostname  }}
  ingressClassName: {{ .Values.ingress.ingressClassName }}
  rules:
    - host: {{ .Values.ingress.keycloakHostname  }}
      http:
        paths:
          - path:  {{ .Values.ingress.httpRelativePath  }}
            pathType: {{ .Values.ingress.pathType }}
            backend: 
              service:
                name: {{ template "common.names.service" . }}
                port:
                  number: {{ .Values.ingress.portnumber }}