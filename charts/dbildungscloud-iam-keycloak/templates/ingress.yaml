apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "common.names.ingress" . }}
  labels: 
    app.kubernetes.io/name: {{ template "common.names.releasename" . }}
spec:
  ingressClassName: {{ .Values.ingress.ingressClassName }}
  rules:
    - host: {{ .Values.ingress.hostname  }}
      http:
        paths:
          - path:  {{ .Values.ingress.httpRelativePath  }}
            pathType: {{ .Values.ingress.pathType }}
            backend: 
              service:
                name: {{ template "common.names.service" . }}
                port:
                  number: {{ .Values.ingress.port.number  }}